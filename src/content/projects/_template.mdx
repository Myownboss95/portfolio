---
# ─────────────────────────────────────────────────────────────────────────────
# HOW TO ADD A PROJECT
# 1. Copy this file, rename it to my-project-slug.mdx (the filename = the URL).
#    (Files starting with "_" are ignored, so this template never publishes.)
# 2. Fill in the frontmatter below and write the case study in the body.
# 3. Save + `git push`. Cloudflare rebuilds the site automatically.
# ─────────────────────────────────────────────────────────────────────────────
title: "Project Name"
role: "Your role, e.g. Technical Lead"
company: "Company / Client"
period: "2024 — Present"
summary: "One or two punchy sentences. This shows on cards and at the top of the page."
stack: ["Laravel", "PostgreSQL", "Redis"]
domain: ["Payments"]          # tags: Payments, Security, KYC, Mobile, Infra...
featured: false               # true = show on the homepage
order: 99                     # lower number sorts higher
sourcePrivate: false          # true = render the "source is private" note
links:
  live: "https://example.com"           # optional
  appStore: "https://apps.apple.com/..." # optional
  playStore: "https://play.google.com/..." # optional
  repo: "https://github.com/you/repo"     # optional (only if public!)
---

## The problem

What needed solving and why it mattered to the business.

## Constraints

The hard parts — scale, compliance, deadlines, third parties, etc.

## What I built

The architecture. Use a diagram or a fenced code block for system flow.

## The decision that mattered

The one or two non-obvious engineering calls you made, and the tradeoff.

## Impact

Outcomes, numbers, what shipped.
